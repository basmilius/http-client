{
    "name": "@basmilius/http-client",
    "description": "Http client for Vue projects.",
    "version": "1.3.0",
    "scripts": {
        "build": "pnpm esbuild && pnpm gentypes",
        "dev": "pnpm esbuild && concurrently -k -c magenta,cyan,green -n code,types,alias 'pnpm esbuild:watch' 'tsc -w' 'tsc-alias -w'",
        "esbuild": "esbuild src/index.ts --bundle --minify --sourcemap --target=esnext --external:luxon --external:vue --platform=browser --format=esm --outfile=dist/basmilius.http.client.js",
        "esbuild:watch": "pnpm esbuild --watch",
        "gentypes": "tsc && tsc-alias"
    },
    "engines": {
        "node": ">=22"
    },
    "files": [
        "dist/*"
    ],
    "type": "module",
    "main": "./dist/basmilius.http.client.js",
    "module": "./dist/basmilius.http.client.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/basmilius.http.client.js"
        }
    },
    "sideEffects": false,
    "dependencies": {
        "object-hash": "^3.0.0"
    },
    "devDependencies": {
        "@types/luxon": "^3.4.2",
        "@types/object-hash": "^3.0.6",
        "concurrently": "^8.2.2",
        "esbuild": "^0.23.1",
        "luxon": "^3.5.0",
        "tsc-alias": "^1.8.10",
        "typescript": "^5.5.4",
        "vue": "^3.5.0"
    },
    "peerDependencies": {
        "luxon": "^3.5.0",
        "vue": "^3.5.0"
    }
}
